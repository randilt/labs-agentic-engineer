{
  "requirements": [
    {
      "id": "REQ-001",
      "statement": "Callers can create, get, and list orders against either the legacy or the modernize endpoint.",
      "criteria": [
        { "id": "AC-001-a", "must": "POST /orders creates an order and returns 201", "method": "e2e" },
        { "id": "AC-001-b", "must": "GET /orders/{id} returns the created order", "method": "e2e" },
        { "id": "AC-001-c", "must": "GET /orders?customerId= lists that customer's orders", "method": "e2e" }
      ]
    },
    {
      "id": "REQ-002",
      "statement": "The modernize sibling matches the legacy sibling on the same requests.",
      "criteria": [
        { "id": "AC-002-a", "must": "Responses from orders-api-next match orders-api for the same create/get/list sequence", "method": "e2e" }
      ]
    }
  ]
}
